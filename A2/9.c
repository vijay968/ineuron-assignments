#include <stdio.h>

int main()
{
    int a;
    char b;
    double c;
    float d;
    printf("\nSize of int is %d", sizeof(a));
    printf("\nSize of float is %d", sizeof(d));
    printf("\nSize of double is %d", sizeof(c));
    printf("\nSize of char is %d", sizeof(b));
    return 0;
}